## 1. Схема сообщений и идентификатор модели

- [x] 1.1 Добавить nullable-поле `modelId` в Prisma-модель `Message`, создать миграцию без backfill старых строк и проверить `bunx prisma validate`.
- [x] 1.2 Расширить параметры `saveMessage` и сохранить `model.modelId` для успешно доставленных обычных AI-ответов; проверить, что входящие и не-AI сообщения получают `NULL`.
- [x] 1.3 Добавить unit-тесты для основной и fallback-модели, старой записи и не-AI исходящего сообщения; проверить `bun run test -- src/__tests__/...` с затронутыми тестами.

## 2. Runtime-аналитика и векторный контекст

- [x] 2.1 Реализовать in-memory состояние счётчиков по московской дате для vector searches, searches with hits, добавленных context messages, попыток/ошибок/успешных AI-ответов и latency; проверить изоляцию дат и сброс в 00:00 МСК.
- [x] 2.2 Инструментировать завершение векторного поиска и AI-цикл так, чтобы счётчики отражали фактически добавленный context и фактический результат генерации; проверить сценарии пустого результата, ошибки и частичного периода после рестарта.

## 3. Durable aggregate queries и snapshot

- [x] 3.1 Добавить запросы для входящих сообщений, уникальных увиденных чатов/пользователей, отвеченных чатов/пользователей и разбивки по личным/групповым чатам с явным bot user ID; проверить self-reply связи и нулевые результаты.
- [x] 3.2 Добавить запросы для `Message.modelId`, новых `UserFact`, использованных `FactImpact`, `QuotaUsage`, существующих покупок/возвратов и активных подписок; проверить дневные границы `Europe/Moscow`.
- [x] 3.3 Собрать единый analytics snapshot из durable и runtime-метрик, явно помечая runtime-значения как неполные после рестарта; проверить, что snapshot не содержит текстов, prompts, фактов и Telegram ID.

## 4. Автоматический и ручной отчёты

- [x] 4.1 Перевести автоматический отчёт на общий snapshot builder и сохранить защиту `DailyAnalytics.reportSentAt`; проверить повторный запуск scheduler после уже отправленного отчёта.
- [x] 4.2 Добавить owner-only обработчик `/analytics` только для private chat и `ANALYTICS_CHAT_ID`; проверить владельца, другого пользователя, групповой чат и ручной вызов после автоматического отчёта.
- [x] 4.3 Зарегистрировать команду скрыто или только в owner-specific command scope и проверить, что она не появляется в общих меню и не отвечает неавторизованным пользователям.

## 5. Формат и регрессии

- [x] 5.1 Обновить текст отчёта: дорогие/дешёвые ответы и model IDs, аналитики, увиденные/отвеченные чаты и пользователи, факты, vector context, успешные/неуспешные ответы, response rate, среднее/p95 latency, image/voice, покупки/возвраты и подписки; проверить русский текст и нулевые значения.
- [x] 5.2 Добавить regression-тесты privacy-safe формата и отсутствия данных `/ask` в обычной аналитике; проверить, что guest mode не начинает сохраняться в `Message` только ради отчёта.
- [x] 5.3 Выполнить итоговую проверку `bun run db:generate`, `bunx prisma validate`, `bun run typecheck`, `bun run lint`, полный `bun run test` и `git diff --check`; убедиться, что изменены только файлы согласованного change.
