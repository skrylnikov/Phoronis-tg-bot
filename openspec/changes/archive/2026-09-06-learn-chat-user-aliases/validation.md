# Проверки реализации — 2026-09-06

## Результаты

- `bun run db:generate` — успешно, Prisma Client 7.9.1. После генерации удалены trailing whitespace в новых строках; существующее форматирование сохранено для минимального diff.
- Все 27 миграций, включая новую аддитивную миграцию, применены к отдельной PostgreSQL 18 с pgvector 0.8.2.
- `bun run typecheck` и `bun run lint` — успешно.
- `bun run test:unit` — 66 файлов, 306 тестов успешно.
- `user-alias.integration.test.ts` — 2 PostgreSQL-теста, 21 assertion: ограничения БД, гонки, retry, пересчёт после удаления источника, порядок owner-команд, восстановление после reject и сохранение настройки после private cleanup.
- `user-alias-flow.integration.test.ts` — 1 сквозной PostgreSQL-тест, 14 assertions: контролируемый результат анализа, повторная доставка, два автора, рейтинг 0.91808, поиск истории и профиля, выбор обращения, prefer/avoid/reject и изоляция чатов.
- `git diff --check` и `openspec validate learn-chat-user-aliases --strict` — успешно.

## Повторный запуск PostgreSQL-проверок

Тестовая база находится в отдельном контейнере `phoronis-alias-test-20260906`; после проверок контейнер остановлен, данные сохранены. Не использовать production DATABASE_URL: интеграционные тесты создают и удаляют синтетические записи.

```sh
docker start phoronis-alias-test-20260906
export DATABASE_URL=postgresql://postgres:alias-test@127.0.0.1:55436/phoronis_alias_test
bun test src/__tests__/user-alias.integration.test.ts
bun test src/__tests__/user-alias-flow.integration.test.ts
docker stop phoronis-alias-test-20260906
```

Сквозной тест запускается отдельным процессом, поскольку подменяет модули модели и Telegram. Он проверяет сохранённые данные и инструменты, а не качество живой модели.

## Незавершённая проверка 5.3

В локальном окружении отсутствует `ROUTERAI_API_KEY`, используемый `src/ai/ai.ts`. Живые вызовы RouterAI и Telegram-сообщения не выполнялись. Качество извлечения и окончательных формулировок ответа остаётся непроверенным; задача 5.3 не отмечена выполненной.

После настройки ключа необходимо проверить на синтетических данных положительные примеры («Санёк, привет» в reply Александру, «называй меня Саша», «не называй меня Шуриком» при существующем «Шурик») и отрицательные («передай Саше привет» в reply Ивану, цитата чужой просьбы, неизвестный адресат, неприятное прозвище). Требуется оценить фактический structured result модели и текст ответа, включая применение запрета уже в текущем ответе. Проверки mocks не заменяют этот этап.

На этапе apply изменения предыдущего OpenSpec change были сохранены; commit, push, синхронизация canonical specs, архивирование и production-развёртывание не выполнялись.

## Архивирование — 2026-09-06

По следующему явному запросу пользователя change архивирован с 15/16 выполненными задачами. Пункт 5.3 остаётся открытым по указанной выше причине. Все три delta specs синхронизированы с canonical specs и проверены; архивирование не означает прохождение живой проверки LLM.
