## 1. Хранение и накопление уверенности

- [x] 1.1 Добавить `UserAlias`, `UserAliasEvidence` и аддитивную миграцию с FK, диапазонами confidence, уникальностью связи/источника и одного предпочтительного обращения. Проверка: миграция на отдельной тестовой PostgreSQL, отклонение недопустимых значений и дубликатов, успешный `bun run db:generate`.
- [x] 1.2 Реализовать нормализацию и валидацию псевдонимов без новых зависимостей. Проверка: существующий Vitest покрывает `ё/е`, регистр, Unicode, пробелы, пустое значение, управляющие символы, предел длины, ID и `@username`.
- [x] 1.3 Реализовать транзакционное сохранение evidence и воспроизводимый пересчёт confidence, дневную дедупликацию по автору, число подтверждений, пороги и сохранение owner overrides. Проверка: оценки `0.8 → 0.84 → 0.872 → 0.8976 → 0.91808`, границы `0.5/0.8/0.9`, независимые авторы и дни Europe/Moscow, удаление источника, повторное и конкурентное сохранение на PostgreSQL не меняют ожидаемый результат.

## 2. Автоматическое извлечение

- [x] 2.1 Дополнить вход анализа ID известных авторов/адресатов и только публичным reply-контекстом; сохранить проверку авторства обычных фактов. Проверка: тест входного prompt исключает private-родителя и неизвестных адресатов, существующие проверки `facts` продолжают проходить.
- [x] 2.2 Добавить `aliases` в structured result существующего LLM-вызова и локальные инструкции извлечения/нейтральности, сохранив `facts` и текст базового `meta-analyzer`. Проверка: синтетические ответы модели проверяют «Санёк, привет» в reply, третье лицо «передай Саше», пустой массив, невалидную уверенность, придуманные ID/источники/имена и сохранение валидных элементов рядом с ошибочным наблюдением.
- [x] 2.3 Подключить сохранение псевдонимов к существующему `USER_MESSAGE_ANALYSIS`, исключив bot-authored источники и бота-адресата. Проверка: tests фонового анализа подтверждают шаг 30 сообщений, прежнюю квоту `ANALYSIS`, отсутствие дополнительного LLM-вызова на каждое сообщение и идемпотентность retry после частичного сохранения.

## 3. Поиск пользователей

- [x] 3.1 Вынести общий resolver из истории и добавить точные/частичные совпадения и псевдонимы текущего чата с безопасной обработкой кандидатов. Проверка: тесты покрывают ID, явный username, полное имя, несколько псевдонимов одного человека, совпадение имени с чужим псевдонимом, двух владельцев с разной уверенностью, неподтверждённую связь, пустой ввод и усечение списка без потери неоднозначности.
- [x] 3.2 Подключить resolver к `search_chat_history`, сохранив `sender` и ограничение публичной истории текущим чатом. Проверка: запрос по «Шурик» выбирает правильного автора в search/user_stats, неоднозначность требует уточнения, исторический автор доступен только в истории своего чата.
- [x] 3.3 Добавить альтернативный `query` и псевдонимы в `get_user_info` с текущими проверками участия до выдачи профиля и списка кандидатов. Проверка: тесты подтверждают совместимость `userId`/пустого ввода, ошибку `query + userId`, отказ при left/kicked/API failure, отсутствие утечки из другой группы/лички и невозможность поиска чужого пользователя из лички.

## 4. Предпочтения и обращения

- [x] 4.1 Реализовать `set_my_alias` для явных действий `prefer`, `avoid_addressing`, `reject_identity`, привязав полномочия к отправителю и чату; зарегистрировать только в writable режиме. Проверка: тесты покрывают предпочтение, запрет без потери поиска, отклонение связи, попытку изменить чужое имя, цитату вместо просьбы, guest/read-only отказ, невалидный ввод и ошибку записи без сообщения об успехе.
- [x] 4.2 Обеспечить транзакционную единственность предпочтения и порядок явных команд, защитить overrides от позднего фонового анализа и cleanup private-сообщений. Проверка: интеграционный тест конкурирующих prefer, старого retry после новой просьбы, обучения после reject, явного восстановления владельцем и сохранения настройки после удаления private-источника.
- [x] 4.3 Добавить выбор обращения: предпочтение владельца, затем нейтральный подтверждённый псевдоним с confidence `>= 0.9` и двумя авторами, затем Telegram-профиль. Проверка: тесты выбора подтверждают приоритет просьбы, исключение запрещённого/сомнительного имени, недостаток авторов, разрешение равенства и fallback.

## 5. AI-контекст и проверки сценариев

- [x] 5.1 Передавать scoped aliases/confidence/обращение/запреты в обычном, гостевом и fallback контексте, а изменения — через существующие update/correction события и результат writable tool. Проверка: тесты контекста подтверждают отсутствие чужих чатов, трактовку имён как данных, изменение уже текущего ответа и сохранение ограничений tools.
- [x] 5.2 Проверить приоритет актуального обращения и отклонений в существующем треде, после compaction и рестарта. Проверка: тест builder сохраняет старые события, добавляет явное исправление/удаление и не восстанавливает запрещённое имя из summary.
- [ ] 5.3 Выполнить сквозной локальный сценарий с синтетическими участниками: два автора подтверждают имя → рост рейтинга → поиск истории → автоматическое обращение → явное предпочтение → запрет → отклонение. Проверка: доступный повторяемый сценарий с контролируемыми ответами модели и Telegram проверяет сохранённые записи и результаты tools; отдельно вручную проверить извлечение и формулировки ответов на положительных и отрицательных примерах, не выдавая mock-проверки за проверку качества LLM.
- [x] 5.4 Выполнить `bun run typecheck`, `bun run lint`, `bun run test:unit`, затронутые PostgreSQL integration checks, `git diff --check` и `openspec validate learn-chat-user-aliases --strict`; зафиксировать результаты и реальные ограничения проверки модели. Проверка: команды проходят, либо конкретный внешний блокер явно записан без отметки соответствующей работы выполненной. Production-развёртывание не входит в этот checklist.
