## 1. Ленивый Telegram stream sink

- [x] 1.1 Изменить обычный поток публикации так, чтобы создание sink не отправляло «Думаю…», а первый непустой update сразу отправлял reply в группе или начинал draft в личном чате; проверить focused-тестами отсутствие вызова placeholder до первого update и немедленную публикацию первого фрагмента.
- [x] 1.2 Сохранить редактирование последующих фрагментов, финальную отправку при отсутствии stream updates, rich/MarkdownV2/plain fallback и безопасную отмену; проверить сценарии успешного, пустого и ошибочного стрима.
- [x] 1.3 Оставить ephemeral-ветку `/ask` с её текущим placeholder и редактированием ответа без изменений поведения; проверить существующий тест `/ask` в `telegram-stream.test.ts` и тест контроллера ask.

## 2. Жизненный цикл typing

- [x] 2.1 Перенести немедленное действие `typing` в обычном AI-контроллере до создания stream sink и сохранить один периодический refresh с очисткой при успехе, ошибке или отмене; проверить, что ошибка Telegram API логируется и не останавливает генерацию.
- [x] 2.2 Добавить раннее best-effort действие `typing` в text/photo-обработку сразу после определения, что ответ нужен, до поиска контекста или распознавания изображения, не включая сообщения без ответа бота; проверить порядок вызовов и отсутствие typing для skipped group message.

## 3. Регрессионное покрытие и проверка

- [x] 3.1 Обновить тесты `TelegramStreamSink` для private/group-публикации первого фрагмента, отсутствия «Думаю…», rich formatting, no-chunk fallback и ошибок до первой публикации; проверить `bun run test -- src/__tests__/telegram-stream.test.ts`.
- [x] 3.2 Обновить тесты обычной text/photo-обработки и сохранить проверки guest/ask/voice-границ; проверить `bun run test -- src/__tests__/process-message.test.ts src/__tests__/guest.test.ts src/__tests__/ask.test.ts`.
- [x] 3.3 Выполнить итоговую проверку проекта: `bun run typecheck`, `bun run lint` и полный `bun run test`; убедиться, что изменены только код и тесты в заявленной области, без Prisma migration и новых зависимостей.
