## 1. Зафиксировать текущие контракты

- [x] 1.1 Составить таблицу всех runtime-caller-ов prompt management, текущих Langfuse prompt names/версий и ожидаемых форматов ответов; проверить её поиском по `getPrompt` и существующими тестами
- [x] 1.2 Добавить regression fixtures для согласованных текстов `text-beautifier`, `voice-summarize`, `chat-generation`, `image-description` и текущего `meta-analyzer` v2; проверить побайтовое совпадение статических частей и корректную подстановку `author`

## 2. Локальный реестр промптов

- [x] 2.1 Создать локальный типизированный реестр пяти промптов с локальной версией и безопасным вычислением короткого hash; проверить, что модуль не зависит от Langfuse и принимает только объявленные переменные
- [x] 2.2 Переключить voice-, image-, chat- и guest-generation на локальный реестр, сохранив текущие модели, схемы, формат Telegram-ответов и обработку ошибок; проверить вызовы focused-тестами
- [x] 2.3 Переключить активный fact-анализатор на локальный snapshot `meta-analyzer` с сохранением дополнительных требований к `facts`, `content`, `type` и `sourceMessageId`; проверить schema/contract-тестом
- [x] 2.4 Удалить runtime-вызовы `langfuse.getPrompt` и неиспользуемый legacy-метаанализатор после проверки всех импортов; проверить `rg`-поиском отсутствие prompt fetching и typecheck отсутствие скрытых callers

## 3. Хранилище и идентичность AI-треда

- [x] 3.1 Добавить Prisma-модели для стабильных настроек AI-треда и append-only context events с уникальными ключами, индексами и JSON payload для immutable snapshots; проверить `bun run db:generate` и валидность migration SQL
- [x] 3.2 Связать тред с существующей reply/session-семантикой и обеспечить сохранение `sessionId` у сообщений, участвующих в AI-треде; проверить сценарии нового root-запроса, reply chain и рестарта процесса
- [x] 3.3 Реализовать транзакционное добавление событий с монотонным sequence и идемпотентным `turnId/eventKind`; проверить повторную доставку и конкурентное добавление без дублей
- [x] 3.4 Сохранить случайные chat-generation rules при создании треда и возвращать их без повторной генерации; проверить одинаковый snapshot настроек до и после восстановления из базы

## 4. Append-only сборка контекста

- [x] 4.1 Разделить сборку chat-generation на стабильные инструкции, настройки треда и историю контекстных событий; проверить, что время текущего turn не меняет стабильный prefix
- [x] 4.2 Добавить сериализацию начального user-context, новых пользователей, fact-delta/correction и retrieval-событий; проверить порядок `initial → retrieval → user → assistant → new retrieval/delta → next user`
- [x] 4.3 Восстанавливать историю из сохранённых событий и обычных сообщений, не пересобирая старые динамические блоки; проверить после имитации рестарта точное совпадение порядка и содержимого
- [x] 4.4 Сохранить текущие privacy/chat filters для vector search и guest mode при добавлении retrieval-событий; проверить focused integration-тестами, что чужие сообщения и факты не попадают в тред
- [x] 4.5 Реализовать компактизацию только при приближении к лимиту контекста с новым cache boundary и актуальным snapshot состояния; проверить тестом, что после boundary новые события продолжают добавляться, а событие сброса наблюдаемо
- [x] 4.6 Заменить усечение user/assistant-сообщений на LLM-саммари; переносить все `INITIAL_CONTEXT`, `USER_CONTEXT` и `CORRECTION` без сжатия, а при ошибке LLM не создавать boundary; проверить сохранение пользовательских данных и fallback полного контекста

## 5. Совместимость транспорта и Langfuse tracing

- [x] 5.1 Оставить системную роль только для верхнеуровневых инструкций, а исторические context-события сериализовать в допустимые обычные сообщения; проверить через текущий AI SDK путь, что system-сообщения не вставляются в середину `messages`
- [x] 5.2 Сохранить Langfuse traces для chat/guest и связанных AI-операций, добавив только локальный prompt version/hash, thread id, cache boundary и usage metadata; проверить trace-тестом отсутствие raw prompt и пользовательского контекста в telemetry fields
- [x] 5.3 Добавить метрики стабильного prefix, изменяемой части, cache read/write и latency с состоянием `unavailable` при отсутствии provider usage; проверить безопасный лог/metric snapshot без полного текста запроса

## 6. Миграция, регрессии и проверка

- [x] 6.1 Сохранить совместимость существующих `User.metaInfo` и миграции в `UserFact`, не удаляя исторические данные; проверить миграционным тестом преобразование старого JSON в факты
- [x] 6.2 Добавить unit-тесты локальных prompt fixtures, thread settings, event ordering, deduplication, restart replay, correction precedence и LLM-compaction fallback; проверить `bun run test:unit -- ...`
- [x] 6.3 Добавить интеграционные сценарии первого/второго/длинного turn для обычного и guest mode и проверить, что последующие запросы имеют общий prefix до последних динамических событий
- [x] 6.4 Запустить полную проверку `bun run typecheck`, `bun run lint`, `bun run test:unit` и `git diff --check`; зафиксировать результаты перед переключением change в implementation
