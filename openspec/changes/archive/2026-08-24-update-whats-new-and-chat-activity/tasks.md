## 1. Модель данных и репозитории

- [x] 1.1 Добавить в Prisma-модель `Chat` поля `active` с default `true` и nullable `inactiveSince`, а в `Message` индекс `(chatId, sentAt)`; создать миграцию с сохранением существующих чатов активными и проверить её через `bunx prisma validate`.
- [x] 1.2 Добавить repository-операции для выборки активных групп, пакетного подсчёта per-chat ответов и голосовых сообщений, деактивации чата и атомарной реактивации по сообщениям после `inactiveSince`; покрыть операции unit-тестами.

## 2. Текст и формирование рассылки

- [x] 2.1 Полностью заменить шаблон What’s New на согласованный русскоязычный текст с web-доступом, контекстом/историей, распознаванием изображений только по запросу, таблицей «было → стало» и скидкой 20% до 31 августа 2026 года, 23:59 МСК; тестом проверить отсутствие старых цен, лимитов, `/terms` и `/paysupport`.
- [x] 2.2 Изменить подготовку broadcast snapshot так, чтобы активные чаты получали собственный текст со статистикой конкретного `chatId`, а preview владельца явно обозначал индивидуальную статистику; проверить два чата с разными счётчиками focused-тестом.

## 3. Доставка и активность чатов

- [x] 3.1 Ограничить цели рассылки активными групповыми чатами и после окончательной ошибки всех fallback-вариантов сохранять `active = false` и `inactiveSince`, не останавливая отправку в остальные цели; проверить успешную доставку, ошибку и итоговый отчёт тестами What’s New.
- [x] 3.2 Добавить ежедневную scheduler-задачу под существующим advisory lock, которая реактивирует только чаты с сообщениями после `inactiveSince`; проверить реактивацию при новом сообщении, сохранение inactive без новых сообщений и регистрацию задачи.
- [x] 3.3 Сохранить owner-only authorization, одноразовое подтверждение, последовательную отправку, обработку kicked/failed chat и completion notification; выполнить regression-тесты существующего broadcast flow.

## 4. Проверка change

- [x] 4.1 Выполнить `bun run lint`, `bun run typecheck`, `bun run test:unit` и `git diff --check`, а также отдельно проверить миграцию и отсутствие попыток отправки в неактивные чаты.
