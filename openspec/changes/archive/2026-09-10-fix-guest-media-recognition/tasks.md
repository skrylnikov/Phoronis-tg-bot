## 1. Фото из reply в гостях

- [x] 1.1 Расширить поиск фото в `handleGuestMessage`: сам запрос, затем `reply_to_message` первого уровня; проверить покрытием сценариев «фото в запросе», «фото в reply», «фото нет» в `src/__tests__/guest.test.ts` и убедиться, что тесты проходят
- [x] 1.2 Читать `summary`-кэш сообщения с фото до резерва IMAGE-квоты и сохранять описание после распознавания; проверить unit-тестом «повторный запрос к распознанному фото не вызывает vision API и не тратит квоту»
- [x] 1.3 Добавить отладочный лог наличия медиа-полей в гостевом запросе и его `reply_to_message`; проверить, что лог пишется при обработке гостевого апдейта

## 2. Голосовые сообщения в гостях

- [x] 2.1 Добавить извлечение аудио (voice/video note) из запроса и из `reply_to_message` с резервом/возвратом VOICE-квоты; проверить unit-тестами «войс в запросе» и «войс в reply» с моком speechkit
- [x] 2.2 Прокинуть транскрипт в `generateGuestResponse` новым опциональным входом и встроить в сообщение генерации; проверить тестом, что транскрипт попадает в контекст модели
- [x] 2.3 Покрыть отказ VOICE-квоты и файл больше 20 MiB коротким русскоязычным ответом через `answerGuestQuery` без вызова распознавания; проверить тестами оба сценария

## 3. Лимиты и проверка

- [x] 3.1 Применить дедупликацию лимитных ответов через `shouldSendLimitNotice` для IMAGE/VOICE в гостях; проверить тестом, что повторный отказ в пределах окна не шлёт нотис заново
- [x] 3.2 Прогнать `typecheck` и `lint` после всех изменений и убедиться, что обе команды завершаются без ошибок
- [x] 3.3 Прогнать полный unit-сьют (`bun run test`) и убедиться, что нет регрессий в обычных хэндлерах фото/войсов
