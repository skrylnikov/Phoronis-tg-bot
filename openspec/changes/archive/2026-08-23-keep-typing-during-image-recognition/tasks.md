## 1. Единый lifecycle typing

- [x] 1.1 Вынести best-effort lifecycle `typing` с немедленным действием, периодическим обновлением примерно раз в четыре секунды, логированием ошибок и идемпотентным `stop`; проверить lifecycle focused-тестами.
- [x] 1.2 Запускать lifecycle в обычных text/photo-контроллерах сразу после `shouldRespond` и останавливать его в `finally` после завершения всего pre-AI и AI-потока; проверить порядок вызовов и отсутствие `typing` для skipped group message.
- [x] 1.3 Передать активный lifecycle в `aiController`, отключив для этого вызова его внутренний heartbeat и сохранив текущее автономное поведение для остальных вызывающих потоков; проверить, что один запрос не создаёт два heartbeat.

## 2. Регрессионное покрытие

- [x] 2.1 Добавить тест длительного распознавания фотографии в reply chain: несколько обновлений `typing` должны произойти до завершения vision-операции; проверить fake timers и deferred promise.
- [x] 2.2 Добавить тест длительного распознавания прямой фотографии с подписью и проверить отсутствие второго независимого heartbeat внутри AI-контроллера; проверить focused-тестом photo-контроллера.
- [x] 2.3 Проверить остановку lifecycle после успешного ответа, ошибки vision API, отказа IMAGE-квоты и других ранних возвратов; сохранить отсутствие `typing` для сообщения группы без обращения к боту и подтвердить это тестами.
- [x] 2.4 Сохранить проверки специальных voice, guest и `/ask` потоков без изменения их доставки и lifecycle; запустить соответствующие существующие focused-тесты.

## 3. Проверка

- [x] 3.1 Выполнить focused-тесты контроллеров и AI lifecycle; убедиться, что они проходят.
- [x] 3.2 Выполнить `bun run typecheck`, `bun run lint` и полный `bun run test`; сохранить успешные результаты всех трёх проверок.
- [x] 3.3 Убедиться, что изменение не затрагивает Prisma-схему, миграции и зависимости; проверить `git diff --stat` и список изменённых файлов.
