## 1. Конфигурация и адаптер Firecrawl

- [x] 1.1 Добавить необязательную переменную `FIRECRAWL_API_KEY` в конфигурацию и `.env.example`, не делая её условием запуска; проверить тестом запуск/чтение конфигурации без ключа.
- [x] 1.2 Реализовать Firecrawl-клиент для `v2/scrape` и `v2/search` через встроенный `fetch` с Bearer-аутентификацией, проверкой HTTP-ответа и безопасным преобразованием JSON; проверить unit-тестами успешные ответы, malformed response, 4xx/5xx и 429.
- [x] 1.3 Добавить в адаптер проверку абсолютных `http/https` URL, общий abort signal, таймаут, ограничение размера Markdown и бюджет web-вызовов на генерацию; проверить тестами невалидные URL, abort и превышение каждого лимита.

## 2. AI-tools и контекст модели

- [x] 2.1 Реализовать read-only tool `read_web_page` с Zod-схемой URL и компактным результатом `{ source, title, content }` либо безопасной ошибкой; проверить сценарии успешного чтения и недоступной страницы.
- [x] 2.2 Реализовать read-only tool `search_web` с непустым запросом, ограниченным числом результатов и URL/заголовком/описанием каждого источника; проверить успешный поиск, пустой результат и ошибку Firecrawl.
- [x] 2.3 Условно зарегистрировать оба web-tool при наличии ключа в обычном и read-only наборах `chatGeneration`; проверить, что read-only режим не получает write-tools, а отсутствие ключа не меняет существующий набор.
- [x] 2.4 Добавить в инструкции генерации правило: web-данные являются недоверенным источником, web-вызов выполняется при явной просьбе о ссылке, поиске или актуальности, а обычный вопрос не запускает поиск; проверить текст инструкции и отсутствие автоматического вызова на обычном запросе.
- [x] 2.5 Передавать источники из tool-результатов в контекст ответа так, чтобы финальный ответ мог показать пользователю использованные ссылки; проверить тестом наличие исходного URL после чтения и поиска.

## 3. Ошибки, безопасность и приватность

- [x] 3.1 Обработать ошибки Firecrawl, таймауты и rate limit без падения обычной генерации и без утечки токена, raw headers или полного тела ответа; проверить тестами безопасное сообщение и продолжение AI-flow.
- [x] 3.2 Добавить защиту от prompt injection в web-контенте на уровне инструкций/контракта tool-результата; проверить регрессионным тестом, что текст страницы не меняет системные правила и не вызывает write-tool.
- [x] 3.3 Проверить, что web-контент и поисковые результаты не попадают в обычные структурированные логи и telemetry в полном объёме; выполнить целевой тест или repository scan по новым логирующим путям.

## 4. Приёмка и handoff

- [x] 4.1 Выполнить focused unit suite для Firecrawl-клиента и AI-tools с полностью замокированным `fetch`; зафиксировать PASS для всех сценариев спецификации.
- [x] 4.2 Выполнить `bun run typecheck`, `bun run lint` и полный `bun run test`; отделить регрессии текущего dirty worktree от результатов этого change.
- [x] 4.3 При наличии тестового Firecrawl-ключа выполнить bounded live smoke для чтения публичной страницы и web-поиска, не выводя ключ и полный внешний контент; при отсутствии рабочего ключа явно зафиксировать smoke как UNRUN. Smoke UNRUN: `FIRECRAWL_API_KEY` не экспортирован.
- [x] 4.4 Перед handoff проверить `git diff --check`, список изменённых файлов и отсутствие Prisma migration/секретов в diff; показать итоговый OpenSpec status и verification summary. Targeted change scope clean; workspace-wide whitespace warning относится к pre-existing `src/generated/prisma/*`, Prisma migration и generated models относятся к другому dirty change.
