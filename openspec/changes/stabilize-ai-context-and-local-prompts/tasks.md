## 1. Зафиксировать текущие контракты

- [ ] 1.1 Составить таблицу всех runtime-caller-ов prompt management, текущих Langfuse prompt names/версий и ожидаемых форматов ответов; проверить её поиском по `getPrompt` и существующими тестами
- [ ] 1.2 Добавить regression fixtures для согласованных текстов `text-beautifier`, `voice-summarize`, `chat-generation`, `image-description` и текущего `meta-analyzer` v2; проверить побайтовое совпадение статических частей и корректную подстановку `author`

## 2. Локальный реестр промптов

- [ ] 2.1 Создать локальный типизированный реестр пяти промптов с локальной версией и безопасным вычислением короткого hash; проверить, что модуль не зависит от Langfuse и принимает только объявленные переменные
- [ ] 2.2 Переключить voice-, image-, chat- и guest-generation на локальный реестр, сохранив текущие модели, схемы, формат Telegram-ответов и обработку ошибок; проверить вызовы focused-тестами
- [ ] 2.3 Переключить активный fact-анализатор на локальный snapshot `meta-analyzer` с сохранением дополнительных требований к `facts`, `content`, `type` и `sourceMessageId`; проверить schema/contract-тестом
- [ ] 2.4 Удалить runtime-вызовы `langfuse.getPrompt` и неиспользуемый legacy-метаанализатор после проверки всех импортов; проверить `rg`-поиском отсутствие prompt fetching и typecheck отсутствие скрытых callers

## 3. Хранилище и идентичность AI-треда

- [ ] 3.1 Добавить Prisma-модели для стабильных настроек AI-треда и append-only context events с уникальными ключами, индексами и JSON payload для immutable snapshots; проверить `bun run db:generate` и валидность migration SQL
- [ ] 3.2 Связать тред с существующей reply/session-семантикой и обеспечить сохранение `sessionId` у сообщений, участвующих в AI-треде; проверить сценарии нового root-запроса, reply chain и рестарта процесса
- [ ] 3.3 Реализовать транзакционное добавление событий с монотонным sequence и идемпотентным `turnId/eventKind`; проверить повторную доставку и конкурентное добавление без дублей
- [ ] 3.4 Сохранить случайные chat-generation rules при создании треда и возвращать их без повторной генерации; проверить одинаковый snapshot настроек до и после восстановления из базы

## 4. Append-only сборка контекста

- [ ] 4.1 Разделить сборку chat-generation на стабильные инструкции, настройки треда и историю контекстных событий; проверить, что время текущего turn не меняет стабильный prefix
- [ ] 4.2 Добавить сериализацию начального user-context, новых пользователей, fact-delta/correction и retrieval-событий; проверить порядок `initial → retrieval → user → assistant → new retrieval/delta → next user`
- [ ] 4.3 Восстанавливать историю из сохранённых событий и обычных сообщений, не пересобирая старые динамические блоки; проверить после имитации рестарта точное совпадение порядка и содержимого
- [ ] 4.4 Сохранить текущие privacy/chat filters для vector search и guest mode при добавлении retrieval-событий; проверить focused integration-тестами, что чужие сообщения и факты не попадают в тред
- [ ] 4.5 Реализовать компактизацию только при приближении к лимиту контекста с новым cache boundary и актуальным snapshot состояния; проверить тестом, что после boundary новые события продолжают добавляться, а событие сброса наблюдаемо

## 5. Совместимость транспорта и Langfuse tracing

- [ ] 5.1 Оставить системную роль только для верхнеуровневых инструкций, а исторические context-события сериализовать в допустимые обычные сообщения; проверить через текущий AI SDK путь, что system-сообщения не вставляются в середину `messages`
- [ ] 5.2 Сохранить Langfuse traces для chat/guest и связанных AI-операций, добавив только локальный prompt version/hash, thread id, cache boundary и usage metadata; проверить trace-тестом отсутствие raw prompt и пользовательского контекста в telemetry fields
- [ ] 5.3 Добавить метрики стабильного prefix, изменяемой части, cache read/write и latency с состоянием `unavailable` при отсутствии provider usage; проверить безопасный лог/metric snapshot без полного текста запроса

## 6. Миграция, регрессии и проверка

- [ ] 6.1 Сохранить совместимость существующих `User.metaInfo` и миграции в `UserFact`, не удаляя исторические данные; проверить миграционным тестом преобразование старого JSON в факты
- [ ] 6.2 Добавить unit-тесты локальных prompt fixtures, thread settings, event ordering, deduplication, restart replay и correction precedence; проверить `bun run test:unit -- ...`
- [ ] 6.3 Добавить интеграционные сценарии первого/второго/длинного turn для обычного и guest mode и проверить, что последующие запросы имеют общий prefix до последних динамических событий
- [ ] 6.4 Запустить полную проверку `bun run typecheck`, `bun run lint`, `bun run test:unit` и `git diff --check`; зафиксировать результаты перед переключением change в implementation
